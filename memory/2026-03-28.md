# Memory — 2026-03-28

## Sand Gallery Fixes

### Firestore URL Bug (Critical)
- **Root cause**: `gallery_categories/{games,apps,tools}/items` — the actual Firestore path the gallery reads from — had broken `url` fields for ALL 15 game/app/tool entries
- Many had full `https://github.com/TheSandemon/...` URLs stored instead of `https://thesandemon.github.io/{repo}/`
- Some had wrong domain (`graviton.github.io` instead of `thesandemon.github.io`)
- Some had garbage URLs like `https://.github.io//`
- **Fixed**: Updated all 15 entries in `gallery_categories/games`, `gallery_categories/apps`, `gallery_categories/tools` with correct `https://thesandemon.github.io/{repo}/` URLs
- Note: Earlier we had also fixed `media` collection but the gallery actually uses `gallery_categories/{category}/items` — different path

### Iframe Embedding Fix
- **Root cause**: GitHub Pages sends `X-Frame-Options: SAMEORIGIN` which blocks iframe embedding
- **Fix**: Pushed `sandbox="allow-scripts allow-same-origin allow-forms allow-popups allow-pointer-lock"` to `ItemDetail.jsx` in sand-gallery repo
- sand-gallery deployment triggered (GitHub Actions run `23658078681`)


### Cron Delivery Fix
- Gallery crons (3 of them) were failing with: `"Channel is required (no configured channels detected)"`
- **Cause**: `delivery: { mode: "announce" }` in isolated sessions has no channel context to announce to
- **Fix**: Changed all 3 crons to `delivery: { mode: "none" }`
- Crons running every 5 min staggered, cycle log at `kaito/gallery-cycle-log.md`

## Agent Events
- **Kairos**: Sand tried to create a second agent named Kairos. `openclaw agents add kairos` command was run but agent was subsequently deleted by Sand. Kairos workspace was cleaned up.
- **NemoClaw**: Sand asked about installing nvidia.com/nemoclaw. It's NVIDIA's sandboxed OpenClaw agent (alpha). Requires WSL2 + Docker (Linux containers). Sand's machine has neither. Skipped.

## OpenClaw vs Claude Code
- Sand asked if I use `.claude` skills/hooks. I do NOT — I'm OpenClaw-native.
- `.claude` directory = Claude Code (desktop app), separate from OpenClaw
- My skills live at: `C:\Users\Sand\AppData\Roaming\npm\node_modules\openclaw\skills\`
- I can spawn Claude Code as a sub-agent via `sessions_spawn runtime:"acp"` if needed

## State File
- `kaito/state.json` — updated throughout the day
- Gallery builder crons: 3 active, staggered every 5 min, delivery.mode: none
- Gallery builder skill upgraded with rigorous gameplay testing protocol
